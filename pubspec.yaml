name: id_card_generator_app
description: 'A new Flutter project.'
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

    # State management
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.3.5
  # IO and formats
  file_picker: ^8.0.0
  excel: ^4.0.6

  # PDF
  pdf: ^3.11.0
  printing: ^5.13.0

  # Networking (optional)
  dio: ^5.5.0
  retrofit: ^4.0.0

  # Models
  freezed_annotation: ^2.4.0
  json_annotation: ^4.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.0
  freezed: ^2.4.0
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true
